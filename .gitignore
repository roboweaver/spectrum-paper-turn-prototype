node_modules/
dist/
coverage/
test-results/
# Generated by scripts/generate-detail-pages.ts from src/data/cards.ts, on the
# predev and prebuild hooks. Build output, not source: the records are what gets
# reviewed. The committed fixtures under public/fixtures/ are deliberately in a
# sibling directory so the generator's wipe cannot reach them.
public/detail/
playwright-report/
blob-report/
playwright/.cache/
.kiro/

# Spec execution telemetry: per-task execution and chat-session IDs with
# timestamps, rewritten on every task run. The plan itself -- requirements.md,
# design.md, tasks.md -- is tracked; this is machine state, not review material.
plans/**/tasks.meta.json
